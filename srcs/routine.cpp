#include "config.hpp"

// Keep the window alive, exiting this function should mean closing the window
static void program_loop(GLFWwindow *window) {

}

// Will call the program loop
void	Rendering(GLFWwindow *window) {

}